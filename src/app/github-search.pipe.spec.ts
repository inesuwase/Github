import { GithubSearchPipe } from './github-search.pipe';

describe('GithubSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GithubSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
